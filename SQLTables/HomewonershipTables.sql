CREATE TABLE homeownership (
    homeownership VARCHAR(20),
    income VARCHAR(50),
    age VARCHAR(50),
    total INT,
    year INT
);

CREATE TABLE owner (
    owner VARCHAR(20),
    income VARCHAR(50),
    age VARCHAR(50),
    total INT,
    year INT
);

CREATE TABLE renter (
    renter VARCHAR(20),
    income VARCHAR(50),
    age VARCHAR(50),
    total INT,
    year INT
);
